#!/usr/bin/env bash
# vim: filetype=zsh

# PS line with pwd, git branch and stuff
source ~/.zsh/util/touchbar.sh
source ~/.zsh/.zsh_ps1

precmd() {
  eval "$PROMPT_COMMAND"
  source ~/.zsh/util/touchbar.sh
}

# fzf commands
source ~/.zsh/fzf_commands/index
