#!/usr/bin/env bash
# vim: filetype=zsh

function is_vpn_on {
  ps aux | grep '[o]penvpn'
}

function parse_node_version {
  if [ -f .nvmrc ]; then
    cat .nvmrc
  fi
}

function parse_git_branch {
  git branch --no-color 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/\1/' -e 's/[()]//g'
}

function parse_git_diff {
  git diff --stat | tail -n1 | sed 's/[^0-9]*/ /g'
}

function parse_git_tag {
  git describe --exact-match --tags HEAD 2>/dev/null
}

function diff_files {
  git diff --shortstat | sed 's/[^0-9]/ /g' | awk '{print $1}'
}

function diff_ins {
  git diff --shortstat | sed 's/[^0-9]/ /g' | awk '{print $2}'
}

function diff_del {
  git diff --shortstat | sed 's/[^0-9]/ /g' | awk '{print $3}'
}

autoload -U colors && colors

precmd() {
  if [ ! -z "$(is_vpn_on)" ];then
    PS1="$fg[black]$bg[green]   %n@%M "
  else
    PS1="$fg[black]$bg[green]   %n@%M "
  fi

  if [ ! -z "$(parse_node_version)" ]; then
    if [[ "$(parse_node_version)" == "$(nvm current | sed s/v//)" ]]; then
      PS1+=" 󰎙 $(parse_node_version) "
    else
      PS1+=" 󰎙 $fg[red]$(parse_node_version) "
    fi
  fi

  PS1+="$fg[green]$bg[yellow]"
  PS1+="$fg[black]   %~ "

  if [ ! -z "$(parse_git_branch)" ]; then
    PS1+="$fg[yellow]$bg[blue]"
    PS1+="$fg[white]  $(parse_git_branch) "

    if [ ! -z "$(parse_git_tag)" ]; then
      PS1+=" $(parse_git_tag) "
    fi

    if [ ! -z "$(parse_git_diff)" ]; then
      PS1+="$fg[blue]$bg[red]"
      PS1+="$fg[black]   $(diff_files)   $(diff_ins)   $(diff_del) "
      PS1+="$reset_color$fg[red]"
    else
      PS1+="$reset_color$fg[blue]"
    fi

  else
    PS1+="$reset_color$fg[yellow]"
  fi

  PS1+="$reset_color$(echo '\n$') "
}
