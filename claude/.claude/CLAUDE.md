Code style
==========
In addition to other code style considerations use my own:

Do not use one-line ifs and elses like this
```
if (something) doThis()
else doThat()
```

Do not use double ternary operators. Prefer splitting the logic into simpler chunks.
